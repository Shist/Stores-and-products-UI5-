<mvc:View
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	controllerName="pavel.zhukouski.controller.StoreDetails">

	<Page title="{i18n>storeDetails}">
		<content>
			<Button text="{i18n>productDetails}" press="onNavToProductDetails" />
			<Button text="{i18n>storesOverview}" press="onNavToStoresOverview" />
		</content>
	</Page>

</mvc:View>