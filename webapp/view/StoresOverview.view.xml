<mvc:View
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:f="sap.f"
	controllerName="pavel.zhukouski.controller.StoresOverview">

	<f:DynamicPage showFooter="true" class="sapUiNoContentPadding">
		<f:title>
			<f:DynamicPageTitle>
				<f:heading>
					<Title text="{i18n>storesList}"/>
				</f:heading>
				<f:breadcrumbs>
					<Breadcrumbs>
						<Link text="{i18n>storesList}"/>
					</Breadcrumbs>
				</f:breadcrumbs>
			</f:DynamicPageTitle>
		</f:title>
		<f:header>
			<f:DynamicPageHeader>
				<SearchField
					search="onStoresSearchBtnPress"
					value="{appView>/currStoresSearchFilter}"
					placeholder="{i18n>search}"
					showRefreshButton="true"/>
			</f:DynamicPageHeader>
		</f:header>
		<f:content>
			<List id="storesList" items="{odata>/Stores}">
				<ObjectListItem
					press="onStorePress"
					type="Active"
					title="{odata>Name}"
					number="{= ${odata>FloorArea} ? ${odata>FloorArea} : '-' }"
					numberUnit="{i18n>squareUnit}">
						<ObjectAttribute text="{= ${odata>Address} ? ${odata>Address} : 'Address is not specified' }" />
				</ObjectListItem>
			</List>
		</f:content>
		<f:footer>
			<OverflowToolbar>
				<ToolbarSpacer/>
				<Button
					icon="sap-icon://add"
					type="Ghost"
					text="{i18n>create}"
					press="loadFormDialog('StoreFormDialog', '/Stores')"/>
			</OverflowToolbar>
		</f:footer>
	</f:DynamicPage>

</mvc:View>